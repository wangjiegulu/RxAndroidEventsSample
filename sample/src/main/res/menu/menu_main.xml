<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".MainActivity">

    <item android:id="@+id/menu_main_add_feed"
          android:title="add feed"
          android:orderInCategory="100"
          app:showAsAction="never"/>

    <item android:id="@+id/menu_main_delete_feed"
          android:title="delete feed"
          android:orderInCategory="100"
          app:showAsAction="never"/>

    <item android:id="@+id/menu_main_action_event_refresh"
          android:title="action refresh"
          android:orderInCategory="100"
          app:showAsAction="never"/>

    <item android:id="@+id/menu_main_action_event_edit"
          android:title="action edit"
          android:orderInCategory="100"
          app:showAsAction="never"/>

    <item android:id="@+id/menu_main_action_event_close"
          android:title="action close"
          android:orderInCategory="100"
          app:showAsAction="never"/>


</menu>
